# 開發進度紀錄

這份文件記錄「做了什麼、遇到什麼問題、怎麼解決的、為什麼那樣選」。

與另外兩份文件的分工：

| 文件 | 內容 |
|---|---|
| [README.md](README.md) | 這個專案是什麼、怎麼跑起來 |
| [SECURITY.md](SECURITY.md) | 資安的威脅模型與每個防護的理由 |
| **PROGRESS.md**（本檔） | **時間軸：進度、踩過的坑、解法** |

> **維護方式**：每完成一個階段、或遇到值得記錄的問題與決策時，就更新這份文件。
> 重點放在「當時為什麼那樣決定」——那是半年後最難回想、也最有價值的部分。

---

## 專案概要

**符文戰場（Riftbound）繁體中文玩家資源站。**

- 線上網址：https://riftbound-tw-sigma.vercel.app
- 原始碼：https://github.com/Arthurguang/riftbound-tw
- 性質：非商業同人專案，遵循 Riot Games「Legal Jibber Jabber」政策
- 第一優先：**資安**

### 長期路線圖

| 階段 | 內容 | 狀態 |
|---|---|---|
| 1 | 起源系列卡牌圖鑑 | ✅ 完成 |
| 1.5 | 三語切換（繁中／简中／英文）＋ 雙語卡面 | ✅ 完成 |
| 1.6 | 部署上線、外部安全掃描 | ✅ 完成 |
| 2 | 規則說明與關鍵字辭典 | ✅ 完成 |
| 3 | 牌組編輯器（合法性檢查、匯出匯入） | ⬜ 未開始 |
| 4 | 抽牌機率計算器、起手練習、開包模擬器 | ⬜ |
| 5 | ~~中英卡名對照~~ | ✅ 已由階段 1.5 涵蓋 |
| 6 | 實體對戰輔助：計分板、戰績記錄、PWA 離線圖鑑 | ⬜ |
| 7 | 規則判例庫、errata 整理、Meta 資訊 | ⬜ |
| 8 | 帳號系統（OAuth）、牌組雲端儲存 | ⬜ 資安門檻大幅提高 |
| 9 | 規則引擎 → 復盤模擬 / 勝率分析 / 組牌建議 | ⬜ |
| 10 | 線上對戰 | ⬜ 依當時法律情況再評估 |

### 目前規模

- 收錄卡牌 **376 張**（起源 OGN 352 + 試煉場 OGS 24）
- 三種語言、兩種卡面
- 測試 **165 項**（51 單元 + 114 端對端）
- 外部安全掃描 **四項全 A+**

---

## 階段 1：卡牌圖鑑

### 做了什麼

卡牌列表、全文搜尋、七組篩選、十種排序、376 個單卡詳細頁、篩選狀態同步到網址、手機版、鍵盤可操作。

### 問題 1：找不到卡牌資料來源

**狀況**　一開始只知道官網有卡牌圖鑑，不確定資料怎麼取得。

**解法**　用瀏覽器檢查官網後發現，官方卡牌圖鑑本身就是靠一支**公開的 JSON API** 取資料，不需要金鑰，也不是爬蟲：

```
https://content.publishing.riotgames.com/publishing-content/v2.0/public/channel/riftbound_website/list/riftbound_gallery_cards?locale=en_US&from=0&limit=200
```

同時發現官方卡圖 CDN 支援即時轉檔（`?w=420&fm=webp&q=78`），744×1039 的 PNG 可以變成約 30KB 的 WebP —— 因此不需要自行代管 250MB 的圖片。

### 問題 2：官方回傳的是 HTML 字串

**狀況**　能力文字是 `<p>[Reaction] (Play any time...)<br />Counter a spell.</p>` 這種 HTML。直接顯示需要 `dangerouslySetInnerHTML`，那是 XSS 的主要入口。

**解法**　寫了一個 fail-closed 的解析器（`scripts/lib/card-text-parser.mjs`），在**建置階段**把 HTML 拆成結構化 token。專案裡因此完全不存在 HTML 字串，也不需要任何 HTML 消毒套件。

解析器只認四種標籤（`<p>` `<br>` `<ul>` `<li>`，已盤點全部 376 張卡確認官方只用這四種），遇到任何其他標籤就**讓建置失敗**。

> 這道防線用 15 種真實 XSS payload 做紅隊測試（`tests/unit/card-text-parser.test.ts`），確認惡意輸入會讓建置爆炸而不是悄悄通過。

### 問題 3：靜態產生與嚴格 CSP 不能並存 ⭐

**狀況**　原本計畫全站靜態產生（建置時就把 HTML 寫死，執行期沒有伺服器邏輯，攻擊面最小）。但實作後**整站的 JavaScript 全被自己的 CSP 擋掉**，畫面變成沒有樣式的裸文字。

**原因**　我們的 CSP 用「每次請求一組隨機通行碼（nonce）」的嚴格模式，只有帶著當次通行碼的程式碼能執行。但靜態 HTML 是建置時就產生的，裡面不可能有當次請求的通行碼。

**只有兩條路**

| 方案 | 結果 |
|---|---|
| 保持靜態，把 CSP 放寬成 `'unsafe-inline'` | CSP 形同虛設，被 Google CSP Evaluator 判為高風險 |
| 改為動態渲染，讓通行碼生效 | 保住嚴格 CSP，代價是每次請求都要渲染 |

**選了後者。** 本站沒有資料庫也沒有外部 API，渲染只是把已打包在程式裡的 JSON 轉成 HTML，沒有任何 I/O，成本極低。而第八階段加入使用者內容後，嚴格 CSP 會從「錦上添花」變成「必需品」，現在做對比之後補容易得多。

### 問題 4：行內樣式也被 CSP 擋

**狀況**　元件裡用 `style={{ backgroundColor: ... }}` 畫領域顏色，全被擋下。CSP 規格中通行碼對 `style="..."` 屬性無效。

**解法**　把樣式搬進 `globals.css` 變成 class，**而不是放寬 CSP**。

> 這是最容易自我破壞的時刻：看到 CSP 報錯就去放寬 CSP。原則是反過來的 ——
> **CSP 報錯代表程式碼有問題，要修的是程式碼。**

---

## 階段 1.5：三種語言

### 做了什麼

介面、卡名、能力文字支援繁中／简中／英文；卡面可在英文與简中之間切換。兩者是**獨立設定**（很多台灣玩家看繁中介面但手上是英文卡）。搜尋跨三語比對 —— 打「阿璃」「阿狸」或「Ahri」都找得到同一張卡。

### 問題 5：官方沒有中文卡牌資料？

**狀況**　實測 Riot 全球官網的 API，所有語系（含 `zh_TW`、`zh_CN`）**都只有英文卡名**。

**解法**　查到中國大陸由 Riot × 闪魂發行，有**官方簡中 API**：

```
https://lol-api.playloltcg.com/xcx/card/searchCardCraftWeb
```

376 張卡 100% 對得上，有官方中文卡名、能力文字、官方卡圖，還多了英文版沒有的**背景敘述**。

繁中則沒有官方數位來源（繁中版 2026-08-07 才上市，只有實體卡）。因此分開處理：

| 內容 | 來源 |
|---|---|
| 卡名 | ChronicleCore 公開頁面的 schema.org 結構化資料（台灣社群整理自官方實體卡） |
| 能力文字 | 官方简中逐字轉繁，介面明確標示「未在地化」 |
| 標籤／卡種／領域 | Riot 官方 Data Dragon 的 `zh_TW` 語系 ＋ 台服正式譯名 |

### 問題 6：取用他站資料的分寸 ⭐

**狀況**　使用者指定從 ChronicleCore（台灣同人站）取繁中資料。但該站的 `robots.txt` 明寫 `Disallow: /api/`，而且他們自己也聲明卡圖「已申請但尚未取得授權」。

**解法**　照做，但把取用範圍縮到最小：

1. **不碰 `/api/`** —— 那是站方對自動化程式的明確技術指令
2. 只讀他們 sitemap 裡列出的公開頁面，取 schema.org JSON-LD（那本來就是給機器讀的格式）
3. **只取卡名**。能力文字我們自己從官方簡中轉繁（發現他們的繁中能力文字本來也是簡轉繁，他們網站上自己標明了）
4. 標籤用台服正式譯名（皮爾托福），比他們的簡轉繁（皮爾特沃夫）**更準確**
5. **完全不引用他們的圖片**，卡圖全走官方 CDN，不佔用他們頻寬
6. 每次請求間隔 800ms，結果寫入版控快取，之後建置不再請求
7. 站上頁尾標明出處

### 問題 7：領域配色我畫錯了

**狀況**　領域的代表色是我憑印象設的，Calm 畫成藍色、Mind 畫成紫色。

**發現**　官方簡中資料直接用顏色命名領域（red / green / blue / orange / purple / yellow），比對後發現 **Calm 是綠色、Mind 是藍色、Body 是橙色**。已修正。

---

## 階段 1.6：部署上線

### 做了什麼

推上 GitHub、部署到 Vercel、設定分支保護、跑完四項外部安全掃描。

### 成績

| 檢測 | 結果 |
|---|---|
| SSL Labs | **A+**（兩台伺服器） |
| Mozilla Observatory | **A+，130/100 分，10/10 通過** |
| securityheaders.com | **A+** |
| Google CSP Evaluator | **零 High severity** |

> 對照：官方網站 playriftbound.com 只有三項安全標頭且**沒有 CSP**，拿不到 A+。

### 問題 8：CI 抓到真的套件漏洞 ⭐

**狀況**　第一次 CI 就失敗，`npm audit` 報告 Next.js 內部夾帶的 `postcss` 8.4.31 有四個高風險問題（XSS、路徑穿越讀取任意檔案）。

**解法**　官方建議升級到 Next 16（大版本、有破壞性變更）。但實際有問題的只有那一個巢狀套件，因此用 npm `overrides` 把全專案的 postcss 統一到已修補的 8.5.26。**結果從「1 高風險 + 1 中風險」變成 0 個漏洞**，且不需要動 Next 主版本。

> 這個漏洞是套件作者最近才公布的，不是我們寫錯 —— 但沒有自動掃描就不會有人發現。

### 問題 9：本機過、CI 紅

**狀況**　修完 postcss 後本機是 0 漏洞，CI 仍然紅。

**排查**　用乾淨 clone 完全複製 CI 流程（`npm ci` + `npm audit`），本機結果是 exit 0、0 個漏洞。所以問題不在程式碼，而在環境：CI 用 Node 22（npm 10），本機是 Node 24（npm 11），兩者處理 `overrides` 的方式不同。

**解法**　把 CI 固定到 Node 24，與開發環境一致。

> **「本機測得過、CI 卻紅燈」是最傷的狀態** —— 久了就會有人開始忽略 CI 的結果，那等於白裝了。

### 問題 10：跨瀏覽器測試抓到真問題 ⭐

**狀況**　把測試擴展到三種瀏覽器引擎後，WebKit（Safari）上整站的 CSS 與 JS 全部載入失敗。

**原因**　CSP 的 `upgrade-insecure-requests` 會把頁面上所有 http 資源強制改用 https。WebKit 連 `http://localhost` 也照做（Chromium 對 localhost 有豁免），本機沒有憑證所以全部失敗。

**解法**　只在「連線本身就是 https」時才送這條指令。正式環境照常生效，本機測試能得到正確結果。

> 這個問題**只測 Chrome 永遠不會發現**。Safari 涵蓋所有 iPhone / iPad 使用者。

### 已知限制：Firefox 無法在本機執行

Playwright 內附的 Firefox 在這台 Windows 電腦上啟動失敗（缺它自己的 `mozglue` 組件，與本專案無關，重裝無效）。

**處理**　Firefox 只在 CI（Linux）執行，本機預設跑 Chromium 與 WebKit。要在本機一起跑：`ALL_BROWSERS=1 npx playwright test`。

> 讓本機測試永遠是紅的，只會讓人開始忽略測試結果 —— 所以寧可明確排除並記錄原因。

---

## 階段 2：規則說明與關鍵字辭典

### 做了什麼

`/rules` 頁面：可搜尋的關鍵字辭典（15 個）、遊戲概要、符號對照、官方文件連結。卡牌能力文字裡的關鍵字可以點進辭典，滑鼠停留顯示官方說明。

### 問題 11：我之前寫錯了五條規則 ⭐⭐

**狀況**　`labels.ts` 裡有我手寫的 15 條關鍵字說明。拿官方印在卡面上的提醒文字比對後，發現**其中五條是錯的**：

| 關鍵字 | 我寫的 | 官方原文 |
|---|---|---|
| Shield 堅守 | 吸收指定數量的傷害 | **防守方時 +1 力量** |
| Deflect 法盾 | 抵銷傷害或效果 | **對手要額外付費才能指定我** |
| Ganking 遊走 | 非預期時機加入戰鬥 | **可在戰場之間移動** |
| Hidden 待命 | 不會被指定為目標 | **先付費蓋著放，之後 0 費當反應打出** |
| Temporary 瞬息 | 效果持續到回合結束 | **下回合開始階段、結算得分前摧毀** |

這些已經上線一段時間。**憑印象寫遊戲規則不可靠，而且錯了不會有人發現** —— 玩家看到「聽起來合理」的說明只會照做。

**解法**　不是「改正這五條」，而是改成**機制上不可能寫錯**：建置腳本掃過全系列 1189 張英文卡與 1261 張简中卡，從官方卡面括號裡的提醒文字自動抽出說明。官方調整措辭時我們會跟著更新。

### 問題 12：自動抽取第一版會「配錯」 ⭐

**狀況**　讓英文與简中各自獨立挑選來源卡，結果英文取到 `[Assault]`（+1 力量）、简中取到 `{强攻2}`（+2 戰力）——**兩邊講不同的東西，但看起來都「對」**。這種錯最難發現。

**解法**　改成「英文與简中的說明必須來自**同一張卡的同一個括號位置**」。規則嚴格後 `Add` 就抓不到了（它的說明沒有緊鄰關鍵字，那張卡還有別的關鍵字）。

**沒有為了湊滿而放寬規則** —— `Add` 在畫面上明確標示「官方卡面未提供說明」並指向官方規則書。其他關鍵字若哪天也抓不到，建置會直接失敗。

### 問題 13：想翻譯官方規則書，但停手了

**狀況**　為了寫規則說明，下載了官方 Core Rules PDF 並寫了解析器（該 PDF 用嵌入式子集字型，需要讀 ToUnicode 對照表才能還原文字）。

**決定停手**　不只是因為難，**更因為就算解出來也不該用**：把 Riot 的規則書整本翻譯放上同人站，正是最容易被要求下架的行為。

**解法**　規則頁只放兩種東西：**每一條都標明出處的事實**，以及官方文件連結。沒有出處的內容不會出現在那一頁。

### 問題 14：CodeQL 抓到我的資安測試本身有漏洞 ⭐⭐

**狀況**　PR 上 CodeQL 回報一個高風險警示，擋住了合併。

**第一次猜錯**　我以為是 `new RegExp(token.source, 'g')`（從變數建構正則式）。改掉後推上去，**仍然是紅的**。

**第二次直接取資料**　從 CodeQL 的 annotation 拿到確切位置 —— 是我自己寫的**資安測試**：

```js
toHaveAttribute('href', /cmsassets\.rgpub\.io.*\.pdf/)
```

這條測試的用意是「確認官方規則書連結指向官方 CDN」。但正則式沒有錨定位置，所以這種網址也會通過：

```
https://evil.example.com/?x=cmsassets.rgpub.io/a.pdf
```

**解法**　新增 `tests/e2e/url-assert.ts`：把網址真正解析出來，逐項比對協定與完整主機名稱。三處網址斷言全部改用它，並補一條「規則頁上每一個外部連結都必須指向官方網域且帶 `noopener`」。

> **一條驗不出問題的資安測試，比沒有測試更危險** —— 它讓人以為已經檢查過了。
>
> 另外，**分支保護在這裡發揮了作用**：它擋住了一個我沒發現的問題進入正式站。

### 問題 15：兩個測試會隨機失敗

**狀況**　兩條測試偶爾逾時。它們會載入中國 CDN 的简中卡圖，而預設的 `page.goto` 會等所有圖片載完，網路一慢就超過 30 秒。

**解法**　改成只等 HTML 解析完成。那兩條測試分別要驗「圖片網址對不對」和「請求送去哪裡」，都不需要圖片真的載完。

> **會隨機變紅的測試比沒有測試更糟** —— 久了就沒有人相信 CI。

### 問題 16：兩個開發中的小坑

- **把函式從伺服器元件傳給用戶端元件** —— 函式無法序列化，導致整個辭典區塊**靜默地渲染不出來**（沒有錯誤訊息）。改成傳字串。
- **搜尋框 `id="keyword-search"` 與關鍵字卡片的 id 前綴衝突** —— 測試抓到的。改名為 `glossary-search`。
- **測試在 hydration 完成前就開始打字** —— 加了明確的「已就緒」標記，測試不必猜時間。

---

## 貫穿全程的幾個原則

這些不是事先訂的，是實際踩過之後歸納出來的：

1. **CSP 報錯代表程式碼有問題，要修的是程式碼**，不是放寬 CSP。
2. **不確定的規則就不要寫**。憑印象寫遊戲規則錯了不會有人發現，改用官方原文自動抽取。
3. **fail-closed**：遇到預期外的資料就讓建置失敗，不要靜默略過。寧可壞掉，也不要讓不明內容上線。
4. **測試通過本身沒有意義**，除非確認過它在該失敗的時候會失敗（曾故意弱化 CSP，確認 7 個測試會變紅）。
5. **會隨機失敗的測試要立刻修**，否則 CI 會失去可信度。
6. **抽取規則寧可嚴到抓不滿，也不要配錯**。抓不到就誠實標示。
7. **每段文字都要能說出它從哪裡來**，介面上也要讓使用者看得到。

---

## 依賴套件更新評估（2026-08-29）

Dependabot 開了 10 個更新 PR。逐一查了官方文件、遷移指南與社群評價，
**然後實際裝上去跑我們的 165 項測試** —— 部落格說得再好，不如自己這套程式碼實測。

### 結論

| 更新 | 決定 | 原因 |
|---|---|---|
| `actions/checkout` 4 → 7 | ✅ 採用 | CI 工具本身，實測通過 |
| `actions/setup-node` 4 → 7 | ✅ 採用 | 同上 |
| `actions/upload-artifact` 4 → 7 | ✅ 採用 | 同上 |
| `github/codeql-action` 3 → 4 | ✅ 採用 | **資安掃描工具升級，直接有益** |
| `google/osv-scanner-action` 2.2.3 → 2.5.1 | ✅ 採用 | **漏洞掃描器升級，直接有益** |
| `@types/node` 24 → 26 | ✅ 採用 | 純型別定義，不影響執行 |
| `vitest` 3 → 4 | ✅ 採用 | 51 項單元測試全過 |
| `eslint` 9 → 10 | ❌ 暫緩 | 生態系不相容（見下） |
| `typescript` 5 → 7 | ❌ 暫緩 | 生態系不相容（見下） |
| `next` 15 → 16 | ❌ 暫緩 | **會破壞我們的 XSS 防護**（見下） |

### 問題 17：Next.js 16 會讓 Trusted Types 失效 ⭐⭐

**狀況**　升級到 Next 16 後建置正常、114 項端對端測試通過了 112 項，
但有一條失敗：卡牌能力文字裡的關鍵字連結**點了不會跳轉**。

**原因**　瀏覽器 console 顯示：

```
This document requires 'TrustedScriptURL' assignment. The action has been blocked.
Failed to set the 'innerHTML' property: This document requires 'TrustedHTML' assignment.
```

**Next.js 16 的用戶端路由器會用 `innerHTML` 與動態腳本網址，被我們的
`require-trusted-types-for 'script'` 擋下**，站內導覽因此失效。Next 15 沒有這個問題。

查證後確認這是 Next.js 的**長期已知問題**（vercel/next.js#13228），到 16.3.3 仍未完全解決。

**決定：留在 Next 15。**

要用 Next 16 就得拿掉 Trusted Types。那是我們**實測有效**的最強 XSS 防線 ——
它讓最典型的攻擊（`innerHTML = '<img src=x onerror=...>'`）連寫入動作本身都被拒絕，
而不只是「寫進去了但不執行」。

拿一個確實在運作的防護去換一個我們用不到新功能的版本號，不划算。
Next 15.5.24 目前仍在維護且沒有已知漏洞（`npm audit` 為 0）。

**重新評估的時機**：Next.js 官方修好 Trusted Types 相容性，或 Next 15 停止安全性更新時。

### 問題 18：ESLint 10 與 TypeScript 7 的生態系還沒跟上

**ESLint 10**　`eslint-config-next`（Next.js 的 lint 設定）依賴的 `@rushstack/eslint-patch`
與 ESLint 10 不相容，直接崩潰。

**TypeScript 7**　這一版是用 Go 重寫的原生編譯器，官方宣稱快 8–12 倍
（實測型別檢查確實只要 1.2 秒）。但：

- CSS 側效匯入的型別宣告失效（`Cannot find module './globals.css'`）
- 尚未提供穩定的程式化 API，`typescript-eslint` 等工具仍只能依賴舊版

速度提升對我們這種規模的專案效益有限（省下十幾秒），不值得為此對抗相容性問題。

**這兩項都等生態系跟上再說。**

### 過程中的一個教訓

用 npm 安裝／退回大版本套件時，它會連帶移除其他套件的相依，
造成「退版後 ESLint 反而壞掉」這種混亂狀態。

**正確做法**：測試每個大版本更新前，先用 `git checkout -- package.json package-lock.json`
加上 `npm ci` 回到乾淨基準，再裝下一個。中途用 `npm install` 反覆升降版會累積髒狀態。

---

## 階段 3：牌組編輯器

### 做了什麼

| 功能 | 說明 |
|---|---|
| 組牌介面 | 依區域分頁選卡（傳奇／主牌組／符文／戰場），搜尋與特性篩選沿用圖鑑的索引 |
| 合法性檢查 | 即時檢查，**每一條錯誤訊息都附官方核心規則條號** |
| 費用曲線 | 主牌組的能量分布長條圖 |
| 收藏記錄 | 標記實際擁有的張數，算出「還需要蒐集哪些牌」 |
| 五種匯出 | 分享網址／牌表文字／CSV／PDF（瀏覽器列印）／PNG 圖片 |
| 收藏備份 | 匯出／匯入 CSV，避免清除瀏覽資料就全沒了 |

**新增的執行期套件：零個。** CSV 和文字自己組字串，PDF 用瀏覽器內建的列印，
PNG 用瀏覽器內建的 Canvas。每多一個套件就多一份供應鏈風險（見問題 8），能不裝就不裝。

**架構沒有改變**：仍然沒有資料庫、沒有帳號、沒有 cookie。
牌組編碼在網址裡，收藏存在瀏覽器的 localStorage。沒有伺服器保管使用者資料，
也就沒有東西可以外洩。

### 問題 19：社群資料有兩個地方是錯的 ⭐⭐

寫合法性檢查時，本來想照著社群整理的「組牌規則」寫。
但因為問題 11 的教訓（憑印象寫規則害我上線五條錯的），這次改成
**先把官方 125 頁的簡中核心規則書抽成純文字，逐條查證**。

結果查出兩個社群普遍寫錯的地方：

| 項目 | 社群常見說法 | 官方條文 |
|---|---|---|
| 主牌組張數 | 「剛好 40 張」 | 103.2：「含**至少** 40 張卡牌的主牌堆」—— 是下限，不是定額 |
| 戰場數量 | 「3 張」 | 485.4.a：**取決於遊戲模式**，1v1 才是 3 張 |

> **⚠️ 這段說法後來被修正了。** 見下方「問題 30」——
> 社群說的「剛好 40 張」其實是**賽事規則**的規定，不是錯的，
> 只是出自另一份文件。我當時只查了核心規則就下結論，那個判斷不完整。

如果照社群說法寫，使用者組了一副 42 張的合法牌組，我們會跳出紅字說它違規。
這種錯誤比「少檢查一項」更糟 —— 它會讓人以為自己的牌組不能用。

程式裡每一條規則都寫上條號，測試裡也寫上條號。任何人都可以自己去對規則書查證。

### 問題 20：特性相符是「子集」不是「交集」 ⭐

雙特性卡（例如同時有熾烈和靈光）能不能放進只有熾烈的牌組？

官方 103.1.b.4：「如果一張卡牌擁有**多種**特性，則它**只能**加入符文特性與其**所有**特性相同的卡組中。」

也就是說：卡的特性必須是傳奇特性的**子集**。只要有一個對不上就不能用。
如果寫成「有任一個相同就可以」（交集），會放行大量不合法的牌組。
這條有專門的測試釘住，正反兩個方向都測。

### 問題 21：卡圖沒辦法畫進匯出的圖片 ⭐

原本想做「有卡圖的漂亮牌組圖」。做不到，而且原因是資安機制本身：

卡圖在 Riot 的 CDN 上。瀏覽器的畫布只要畫進跨網域的圖片就會被標記為
「已污染（tainted）」，之後要把畫布轉成檔案會直接被擋 ——
這是瀏覽器防止網站偷讀其他網站圖片內容的保護機制，要繞過它必須那個 CDN
主動配合回傳 CORS 標頭，不是我們能控制的。

與其做一個「有時候成功、失敗原因還很難懂」的功能，不如做一個**必定成功**的
文字版牌表圖。貼到 Discord 或 LINE 一樣看得懂。

### 問題 22：WebKit 測試抓到一個真的缺陷 ⭐

「開啟收藏記錄」這個開關，原本是從「收藏是不是空的」推導出來的。
Chromium 上看起來沒問題，但 WebKit 的測試會失敗。

查下去發現是真的壞了：使用者打開開關、還沒標記任何卡之前，只要加一張牌到牌組
（會更新網址），元件重新掛載，開關就自己關掉了。

**使用者的偏好設定是獨立的狀態，就該獨立儲存**，不能從資料反推。
改成用另一個 localStorage 鍵記住這個開關。

這是跨瀏覽器測試第二次抓到真問題（第一次是問題 10）。

### 不可信輸入的處理

這個階段第一次有「使用者提供的資料」進入程式，三個來源全部當成不可信：

| 來源 | 風險 | 做法 |
|---|---|---|
| 分享網址 | 任何人都能編造 | 每個卡片代碼都要比對真實卡片，認不得就丟棄並回報數量；張數限制 1–99；最多 300 段 |
| localStorage | 使用者可自行編輯 | 讀取時逐項驗證卡號存在、張數是 0–999 的整數 |
| 匯入的 CSV | 使用者提供的檔案 | 檔案 2MB 上限；逐列驗證；無法辨識的列略過並回報，不讓整份失敗 |

三者都有專門的測試，包含原型污染（`__proto__`、`constructor`）、
CSV 公式注入（`=cmd|...`）、腳本注入、超長輸入。

### 這個檢查「不」涵蓋什麼

介面上明白寫出來：**只檢查核心規則的構築限制，不包含賽制規定（禁卡表、可用系列）。**

那需要另一份會隨賽制變動的官方文件。與其猜，不如誠實說我們沒檢查 ——
讓使用者知道參加正式賽事前要自己再對一次官方賽事規則。

---

### 階段 3 補強：收藏與牌組合併成同一份檔案

原本匯出是兩份檔案：牌表一份、缺卡清單一份。實際帶去卡店時要在兩份之間切換，
很不順手。改成**開啟收藏記錄後，每一種匯出格式都在同一份檔案裡同時呈現牌表與缺卡**。

| 格式 | 怎麼呈現 |
|---|---|
| CSV | 多出「擁有」「還缺」兩欄。在 Excel 對「還缺」排序就是採購清單 |
| 純文字 | 缺的那一列直接標「← 有 1，缺 2」，最後附上彙整清單 |
| PNG | 每列右側標「有 N · 缺 N」，最後一個區塊是缺卡清單 |
| PDF／列印 | 同上，缺卡印在同一張紙上 |
| 分享網址 | **不含收藏** —— 見下方說明 |

沒開啟收藏記錄時，匯出的檔案跟以前完全一樣，不會多出任何欄位。

**為什麼分享網址不帶收藏**：收藏是你個人的資產清單，不該跟著連結散出去。
而且這樣有個更好的效果 —— 別人打開你分享的牌組時，看到的是**他自己**還缺什麼。

**CSV 刻意維持單一張表**（不是牌表一張、缺卡一張疊在同一個檔案裡），
這樣 Excel 的排序與篩選才會正常運作。同一個卡名出現在多列時（異畫版、
或選定英雄同時列在主牌組），數字只填在第一列，其餘留白 ——
否則把「還缺」整欄加總會重複計算。

### 問題 23：缺卡計算原本有兩份實作 ⭐

合併的時候發現「依卡名彙總」這段邏輯同時存在於 `collection.ts` 和新的匯出程式裡。
兩份實作遲早會改一邊忘另一邊 —— 而且這正好是最容易出錯的地方
（異畫版同名、選定英雄同時算在主牌組裡）。

已整併成單一個 `annotateRows()`，測試也全部改測它。
另外抽出 `deckNeeds()` 讓「牌組實際需要幾張」只有一個定義。

### 問題 24：列印會多印出好幾張空白紙 ⭐

列印版面原本埋在元件樹深處，只能用 `visibility: hidden` 藏住其他內容 ——
但那個屬性**仍然佔版面高度**，所以印出來會跟著一大片空白，變成好幾張白紙。

改用 React portal 把列印版面掛到 `<body>` 底下，就能用 `display: none`
真正把其他東西收掉。順帶也發現 Tailwind 的樣式重置會讓列印時的標題跟內文一樣大，
列印樣式裡要自己指定回來。

現在有測試直接產生 PDF 並斷言**只有一頁**，這種問題不會再悄悄跑掉。

### 問題 25：殘留的舊伺服器讓我誤判 ⭐

改完之後測試整批失敗，看起來像是頁面完全沒有互動。查了很久，最後發現
Playwright 的 `reuseExistingServer` 接到了我先前手動啟動、還在跑舊建置的伺服器。
把埠清乾淨後，同一份程式碼就全部通過。

**教訓**：在 Windows 上 `pkill` 不見得殺得掉 node 行程。
懷疑「明明沒改到卻壞了」的時候，先確認測試打的是不是自己以為的那個伺服器。

---

## 階段 4：機率計算

### 做了什麼

| 功能 | 說明 |
|---|---|
| 快速計算 | 牌組張數／放幾張／抽幾張／至少幾張 → 精確機率與完整分布 |
| 選定英雄開局機率 | 開局 4 張抽到的機率，以及依官方順序計算的手牌調度後機率 |
| 逐回合抽到機率 | 主牌組每一種卡，開局到第 5 回合的累積機率 |
| 資源曲線 | 每回合有幾張符文、有多少比例的牌付得起 |
| 符能特性檢查 | 符文牌組沒有對應特性的卡會被標出來 |

從牌組編輯器按「計算機率」就會把牌組帶過來 —— 用的是同一組網址編碼，不需要另外傳資料。

### 為什麼這個階段值得先做

**因為數學可以百分之百驗證。** 抽牌機率有封閉形式的公式（超幾何分布），
算出來的數字可以拿計算機自己驗算，也可以用外部已知答案當測試基準。
不像勝率模擬，需要一整套規則引擎才談得上準確。

測試裡刻意用**外部可查證的已知答案**當基準（撲克牌的經典題目、手算的組合數），
而不是「跑一次程式，把輸出當成期望值」—— 後者只能證明程式沒變，不能證明程式是對的。

### 問題 26：組合數會大到一般數字算不準 ⭐

C(60, 30) = 118,264,581,564,861,424，已經超過 JavaScript 一般數字能精確表示的
上限（9,007,199,254,740,991）。直接用浮點數算，牌組稍大一點結果就開始失真。

改成**全程用 BigInt 做精確整數運算**，只在最後一步除法時才轉成小數。
組合數也用乘除交替的寫法（而不是先算三個階乘再相除），每一步都保證整除，
數字不會膨脹到不必要的大小。

測試裡直接斷言 C(60, 30) 的完整數值，以及它確實超過安全整數上限。

### 問題 27：手牌調度的順序會影響機率 ⭐

官方的調度流程（117.1–117.3）順序很特別：

1. 從手上挑最多兩張**擱置**
2. 抽等量的牌
3. **最後**才把擱置的牌洗回牌組

也就是說補抽的牌是從**不含那兩張擱置牌**的牌堆裡抽的。
很多牌組工具沒有處理這個順序，算出來會略有偏差。

### 問題 28：先手後手的資源不一樣（485.7）

1v1 規則明文寫「第二個行動的玩家，在自己遊戲中首個召出階段從自己的符文牌堆
額外召出一張符文」。所以資源曲線兩邊不同，介面上要能切換 ——
不能只做一條曲線就當成通用。

### 問題 29：受控數字輸入框在負載下會掉按鍵

端對端測試間歇性失敗：輸入「52」偶爾只吃到一個鍵，欄位變空被夾成 0，
算出來的機率當然不對。改成**輸入後驗證欄位真的變成預期的值**，
不成立就整段重來（Playwright 的 `expect(...).toPass()`）。

### 這個工具「不」算什麼 —— 介面上明白寫出來

| 項目 | 誠實的說明 |
|---|---|
| 抽牌機率 | **精確**。可自行驗算 |
| 資源曲線 | 是**下限**。回收符文取得符能後那張符文永久離場（164.2.b），實際會更緊 |
| 卡牌能力 | **完全沒算**。額外抽牌、資源加速、搜尋牌庫都需要規則引擎 |
| 勝率 | **不提供**。要嘛需要引擎自動對打幾千局，要嘛需要記錄真實對局 |

原本規劃裡的**開包模擬沒有做**：官方核心規則書沒有記載補充包的內容組成，
我找不到可以查證的官方來源。與其猜一個比例讓使用者以為是真的，不如先不做。
日後找到官方產品資訊再補。

---

## 階段 4 追加：匯入、備牌、符文機率

### 做了什麼

| 功能 | 說明 |
|---|---|
| 匯入牌組 | 貼上牌表文字或 CSV 就能產生牌組，也可以上傳檔案 |
| 備牌區 | 依賽事規則的備牌，含跨主牌組的同名卡上限檢查 |
| 符文機率 | 各特性符文逐回合召出的機率 |

### 問題 30：我先前對「剛好 40 張」的判斷不完整 ⭐⭐

做備牌功能時要查賽事規則，於是把官方的 Tournament Rules PDF 也抽出來讀。
結果發現我在階段 3 說「社群說剛好 40 張是錯的」**這個判斷不完整**：

| 文件 | 條號 | 規定 |
|---|---|---|
| 核心規則 | 103.2 | 主牌組**至少** 40 張 |
| 賽事規則 | 601.1.b | 賽事中主牌組必須**恰好** 40 張 |

兩份文件都是對的，適用場合不同。社群講的是賽事構築 —— 他們沒說錯，
是我只查了核心規則就下結論。

**教訓**：查證的時候要問「還有沒有別的官方文件也管這件事」。
查了一份就宣稱別人錯，跟憑印象寫規則其實是同一種錯誤，只是隱蔽一點。

現在介面上兩條都顯示：超過 40 張時標為**提醒**（核心規則允許，賽事不允許），
而不是錯誤。同時也補上先前漏掉的一條：賽事規則 402.1 要求
**3 張戰場的名稱各不相同**。

### 問題 31：備牌的規定要看兩份文件

備牌完全不在核心規則裡，全部在賽事規則：

| 條號 | 規定 |
|---|---|
| 403.1 | 只有**某些**賽事才有備牌 |
| 601.1.c.1 | 備牌**10 張以內**（是上限，不是固定 10 張） |
| 601.1.c.2 | 備牌只能放「主牌組放得下」的卡（不能放傳奇、符文、戰場） |
| 601.1.c.3、403.3 | **同名卡上限是主牌組與備牌合計** |
| 403.4 | 換牌必須 1 換 1 |

最後那條容易忽略：主牌組已經 3 張，備牌再放 1 張就超了。
所以同名卡的檢查改成跨主牌組與備牌計算。

因為備牌只有賽事才有，相關問題一律標為「提醒」而不是「錯誤」。

### 問題 32：符文也是隨機的，這點常被忽略 ⭐

原本以為符文是玩家自己選的。查了才發現不是：

| 條號 | 內容 |
|---|---|
| 114 | 主牌堆與**符文牌堆都要洗牌** |
| 430.1 | 召出是從符文牌堆**頂部**抽取 |
| 108.5.d | 符文牌堆的順序是隱密資訊 |

所以「第 N 回合我有沒有需要的顏色」跟抽牌一樣是超幾何分布問題，
母體是符文牌組的 12 張。雙色 6/6 的牌組，先手第 1 回合召 2 張，
至少 1 張特定顏色的機率是 1 − C(6,2)/C(12,2) ≈ 77.3%。

介面上可以選「至少幾張」，因為一張符文只換得到 1 點符能（164.2.b），
符能費用 2 點以上時要問的是「湊不湊得齊」。

### 問題 33：官方兩份 PDF 的內部格式不一樣

核心規則的內容串流用 `(文字) Tj`，賽事規則用 `<十六進位> Tj`。
原本的抽取器只認前者，對賽事規則回傳 0 頁。擴充後 50 頁全部讀得出來。

（這支腳本只在暫存目錄，用來讀懂官方規則，不會把原文放進專案。）

### 匯入「不」支援什麼 —— 介面上寫明理由

| 格式 | 支援？ | 說明 |
|---|---|---|
| 純文字牌表 | ✅ | 「3 卡名」「3x 卡名」「卡名 x3」；繁中／简中／英文／卡號都認 |
| CSV | ✅ | 本站匯出的格式最精確（有區域欄） |
| 圖片 | ❌ | 要文字辨識。**辨識錯的時候不會報錯**，會安靜給你一副錯的牌組 |
| PDF | ❌ | 要帶大型解析器進來，違背零執行期套件原則；同一副牌用 CSV 或網址即可 |

認不出來的行一律列出來給使用者看，不猜。同名卡多版本時固定取卡號最小的那張。

### 問題 34：殘留的測試伺服器又咬我一次

跟問題 25 同一個原因。這次直接關掉 Playwright 的 `reuseExistingServer` ——
埠被佔用時它會明白報錯，那比安靜地對著舊建置測試好得多。

### 順帶：分享網址的格式升到版本 2

多了備牌那一段。**版本 1 的舊連結仍然能開**（解出來備牌為空），
不然先前分享出去的連結會突然失效。這條有測試釘住。

---

### 問題 35：選定英雄 —— 規則要求，但我們刻意降成提醒

使用者反映「尚未指定選定英雄」跳紅字很煩，認為規則沒有強制。

查證結果是**規則確實有要求**：

| 條號 | 原文 |
|---|---|
| 核心規則 103.2 | 「含至少 40 張卡牌的主牌堆：**一張選定英雄單位**，以及其他單位、裝備、法術卡牌。」 |
| 核心規則 103.2.a.1 | 「遊戲開始時，這張卡牌將置於英雄區域。」 |
| 賽事規則 402.1 | 主牌組恰好 40 張「including a chosen champion」 |

核心規則裡 22 處提到「選定英雄」，沒有任何一條寫「可以不指定」。
資料面也查過：40 張傳奇每一張在 Origins 裡都至少有一張可搭配的英雄，
不存在「想指定也指定不了」的情況。

**把證據攤開給使用者看之後，由他決定降成提醒。** 訊息裡保留條號並寫明
「官方規則要求主牌組包含一張選定英雄，正式賽事會檢查這一項」——
降低干擾，但不讓人誤以為不用管。

順帶修掉一個真的噪音：**還沒選傳奇時不再顯示這條**。
沒有傳奇就無從指定英雄（103.2.a.2 要求標籤一致），那時候提醒毫無意義。

另外調整了摘要的措辭：有提醒時顯示「✓ 沒有違規，但有 N 項提醒」，
而不是「✓ 符合核心規則的構築限制」—— 後者跟提醒內容互相矛盾。

「指定了不合規的英雄」仍然是**錯誤**：那是主動選錯，跟還沒選是兩回事。

### 問題 36：機率頁少了掛載完成的標記

端對端測試間歇失敗：輸入框的值改了，畫面上的機率卻不動。
原因是測試在 React 接手之前就打字 —— DOM 的值變了，React 的狀態沒變。

牌組頁早就有 `data-deck-ready` 這個標記，機率頁漏了。補上之後就穩定了。
**這類「畫面看起來對、狀態其實沒更新」的問題，靠重跑測試是查不出來的。**

---

## 階段 5：對局復盤板

### 做了什麼

擺出當下的盤面 —— 雙方牌組、手牌、場上、廢牌堆、放逐 —— 然後從這個局面
算出精確的數字：牌堆裡還剩什麼、再抽幾張抽到某張卡的機率、手上哪些牌
現在的符文付得起。盤面編在網址裡，複製網址就能分享這個局面。

### 這個工具的界線 —— 頁面上明白寫出來

| 項目 | 說明 |
|---|---|
| 從盤面算數字 | ✅ 精確，可自行驗算 |
| 告訴你最佳解 | ❌ 需要看得懂 376 張卡能力、能執行戰鬥與據守的規則引擎 |
| 判斷動作合不合法 | ❌ 「付得起的」只比較符文張數與費用，不檢查時機、目標、能力限制 |

沒有引擎卻跳出「建議」，那個建議是編的。這條界線有端對端測試釘住 ——
頁面上必須出現這句話。

### 區域模型全部照官方規則

| 條號 | 區域 |
|---|---|
| 107.1 | 基地（你控制的常駐牌與符文） |
| 108.2 | 廢牌堆 |
| 108.6 | 放逐區域 |
| 108.7 | 手牌 |
| 133.4 | 選定英雄開局就在英雄區域，**不在牌堆裡** |

### 問題 37：對手的手牌該怎麼處理 ⭐

一開始想讓使用者直接填對手手牌，但那是作弊視角 —— 真實對局裡你看不到。

規則給了答案：

| 條號 | 內容 |
|---|---|
| 108.7.c | 玩家的手牌為**私密資訊** |
| 108.7.e | 但玩家的**手牌數量為公開資訊** |

所以對手那側預設只記「張數」：你一定知道對手有幾張，只是不知道是什麼。
確實知道某張牌在對手手上時才填內容。

這個設計讓算出來的機率是「以你實際知道的資訊為基礎」，
而不是一個在真實對局中不可能有的視角。而且它也影響計算 ——
不知道內容的手牌只能從牌堆的**總張數**扣掉，不能指定扣哪一張。

### 問題 38：巢狀編碼的分隔符撞在一起 ⭐

盤面編碼包著牌組編碼，而我一開始兩層都用 `|` 當分隔符。
結果切開盤面時，牌組裡的 `|` 讓整份錯位 —— 對手的牌組整個解不出來。

改成三層互不重複：`!`（盤面）> `~`（一方）> `|`（牌組）> `.`（一疊）。

**教訓**：巢狀編碼一定要先確認內層沒有用到外層的分隔符。
這個 bug 是來回測試抓到的 —— 如果只測單向編碼就會漏掉。

### 問題 39：擺錯不自己修正

盤面上某張卡的張數超過牌組裡的張數時（例如廢牌堆放了 4 張但牌組只有 3 張），
程式**不會**自己調整，而是明白指出來。

安靜修正只會讓使用者以為擺對了，然後拿著錯的機率去研究。

同理，盤面出現不屬於這副牌組的卡時也會提醒 ——
遊戲中確實可能發生（指示物、被效果加入的卡），但它會讓牌堆計算失準。

### 問題 40：又是網址競態

端對端測試複製分享網址時，`router.replace` 還沒寫完。
跟階段 3 的問題一樣，解法也一樣：等網址真的變了再取。
這次直接用 `expect.poll(() => page.url()).not.toBe(before)`，
比斷言特定字串穩健。

---

### 問題 41：功能在，但使用者找不到 ⭐

使用者反映「沒看到地方可以添加符文」。查下去發現**功能其實在** ——
符文本來就在候選卡清單裡，但要先把「加到」切成「基地」才行。

**這是介面設計的問題，不是使用者沒找到。** 符文張數是復盤時最關鍵的
數字（決定這回合付得起什麼），不該藏在通用的加卡流程裡。

改成獨立的符文控制：牌組裡有幾種符文就列幾列，各自加減，
另外一鍵「補到 N 張」依牌組比例分配（例如熾烈 8 張、翠意 4 張的牌組
要補 8 張時，會分成 5 + 3）。

**教訓**：「使用者找不到」跟「功能不存在」在體驗上是同一件事。

### 問題 42：復盤少了戰場，而戰場是「位置」

使用者反映要能設定目前是什麼戰場。查規則發現這比想像中重要：

| 條號 | 內容 |
|---|---|
| 198.1 | **位置包括戰場和基地** |
| 198.2 | 常駐牌的位置是該常駐牌的**屬性** |
| 485.4 | 1v1 場上有**兩處**戰場 |
| 485.4.a、485.5 | 各由一名玩家從自己構築時放的 3 張裡選一張帶進來 |

所以不只是「選一張戰場」，而是盤面本來就少了一個維度 ——
單位是「在某個位置上」的，而位置有三種：基地、戰場一、戰場二。

補上之後：戰場區域可以從雙方牌組各選一張（並顯示該戰場的能力文字），
單位可以在基地與兩處戰場之間移動。

### 順帶：盤面編碼升到 b2

多了戰場區域與兩處戰場上的單位。**b1 的舊連結仍然能開**，
解出來戰場為空 —— 先前分享出去的復盤連結不會失效。這條有測試釘住。

---

### 問題 43：盤面少了傳奇與英雄區域

使用者指出：傳奇與選定英雄開局就擺出來，理論上不該在牌堆裡。

查證後：**計算本來就是對的**（傳奇根本不在主牌組裡 —— 103.1 與 103.2 是
分開的兩件事；選定英雄也有扣掉）。但**盤面上完全沒有顯示這兩個區域**，
使用者沒辦法確認、也沒辦法在匯入沒推斷出英雄時自己指定。

補上之後：

| 條號 | 做法 |
|---|---|
| 107.4 | 傳奇區域**只顯示不能動** —— 107.4.d 明文說傳奇卡不能從此區移除、移動或移位 |
| 108.3、103.2.a.1 | 英雄區域成為一個真正的區域，匯入時自動把選定英雄擺進去 |
| 108.3.d | 選定英雄可以從英雄區域打出 —— 所以它能移到基地或戰場 |

順帶把「選定英雄扣一張」的特例拿掉了：現在它就是英雄區域那一疊的內容，
由通用的區域計算負責。留著特例會變成扣兩次。

### 問題 44：備牌不該出現在盤面上

備牌在對局進行中**不在場上** —— 賽事規則 403.4 說它只在**局間** 1 換 1
換進主牌組，403.5 說第一局根本不能用備牌。

原本盤面的候選卡清單把備牌也列進去了，那是錯的。

移除之後又發現一個新需求：要復盤第二、三局，得先把換牌結果反映出來，
不然牌堆算的是第一局的組成。所以加了「局間換牌」面板，可以在主牌組與
備牌之間搬卡，並即時顯示前後張數讓使用者對照 403.4.c（換完仍要符合張數要求）。

刻意**不強制 1 換 1** —— 換到一半的中間狀態本來就會不平衡，
擋著只會讓人沒辦法操作。

### 問題 45：多一步就是慢一步

原本刻意讓卡片只能先加到基地或手牌，再用搬移按鈕移到戰場，
理由是「比較貼近實際流程」。使用者反映復盤時這樣太慢。

**使用者是對的**：復盤是重建一個已知的局面，不是模擬遊戲流程 ——
直接選目的地才對。六個區域現在都能直接選。

---

## 待辦事項

- [ ] **Next.js 16**：等官方修好 Trusted Types 相容性再評估（追蹤 vercel/next.js#13228）
- [ ] **ESLint 10 / TypeScript 7**：等 `eslint-config-next` 與 `typescript-eslint` 支援
- [ ] `Add` 關鍵字缺官方說明（已在介面標示，等官方在後續系列補上）
- [ ] 繁中能力文字目前是簡轉繁，官方若推出繁中資料應改用官方版
- [ ] Firefox 無法在本機執行（環境問題，CI 有涵蓋）
- [ ] 開包模擬：需要官方補充包內容組成的可查證來源

---

## 更新紀錄

| 日期 | 內容 |
|---|---|
| 2026-08-29 | 階段 1、1.5、1.6、2 全部完成並上線；建立本文件 |
| 2026-08-29 | 評估 10 個依賴更新：採用 7 項（含兩個資安掃描工具升級），暫緩 3 項；Next.js 16 會破壞 Trusted Types |
| 2026-08-30 | 階段 3 牌組編輯器完成：合法性檢查（附官方條號）、收藏與缺卡清單、五種匯出格式；查證官方規則書修正兩個社群流傳的錯誤說法 |
| 2026-08-30 | 收藏與牌組合併成同一份匯出檔案；整併重複的缺卡計算；修正列印會多印空白頁的問題 |
| 2026-08-30 | 階段 4 機率計算完成：超幾何分布（BigInt 精確運算）、逐回合抽到機率、依官方資源規則的順暢度分析；明確不提供勝率並說明原因 |
| 2026-08-30 | 追加牌組匯入、備牌區、符文機率；查賽事規則後更正「剛好 40 張」的說法，並補上戰場同名檢查 |
| 2026-08-30 | 選定英雄改為提醒（規則確實要求，但由使用者決定放寬）；補上機率頁的掛載標記 |
| 2026-08-30 | 階段 5 對局復盤板：盤面編輯、從當下局面算機率；對手手牌依 108.7 只記張數；明確不提供「最佳解」 |
| 2026-08-30 | 復盤板補上獨立的符文控制與戰場區域；依規則 198.1 加入「位置」維度（基地／兩處戰場） |
| 2026-08-30 | 復盤板補上傳奇／英雄區域與局間換牌；備牌不再出現在盤面；六個區域都能直接選 |
