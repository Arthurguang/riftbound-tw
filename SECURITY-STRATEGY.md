# 資安策略

> 這份文件跟 [`SECURITY.md`](SECURITY.md) 不一樣，兩份都要有：
>
> - **`SECURITY.md`** 是「**我們當初為什麼這樣設計**」—— 威脅模型、防護措施、踩過的取捨。
> - **這份**是「**上線前、改動前，我要問自己什麼**」—— 一份可以重複跑的盤點清單，
>   加上目前查證出來的現況與缺口。

## 這份文件的框架從哪來

框架取自 Gary Chen 的影片〈給非技術人員的資安教學，Vibe Coding 必學的基本功〉
（<https://youtu.be/t9WA-BkLUps>）所提出的**上線前五個問題**。

**誠實說明**：這五個問題是從該影片的官方說明欄與章節時間戳取得的，
不是從影片內容轉述 —— 影片各段講了什麼細節，這份文件沒有引用。
下面每一題的**答案**都是對照本專案實際狀況得出的，並附上查證方式。

五個問題：

1. 哪些東西被偷會出大事？
2. 誰能碰哪些資料？
3. 哪一段是我管不到的？
4. 哪些規則絕對不能被打破？
5. 外面有哪些門是開著的？

---

## 一、哪些東西被偷會出大事？

**先講結論：這個網站本身幾乎沒有值得偷的東西。真正的要害在網站之外。**

這不是自我感覺良好，是架構決定的 —— 沒有後端、沒有資料庫、沒有帳號、
沒有 cookie、沒有伺服器端的使用者資料。攻擊者打下整個網站，
拿到最有價值的東西是**一份本來就公開的卡牌 JSON**。

所以資產盤點的重點必須**往外移**：

| 資產 | 被拿走會怎樣 | 現況 |
|---|---|---|
| **GitHub 帳號** | 可以改程式碼、改 CI、埋後門，然後自動部署上線 | ⚠️ 需你本人確認 2FA |
| **Vercel 帳號** | 可以直接改部署內容、改環境變數、換網域指向 | ⚠️ 需你本人確認 2FA |
| **網域控制權**（若日後接自訂網域） | 可以把整個網站導到別的地方 | 目前用 Vercel 子網域 |
| 原始碼 | 公開儲存庫，本來就看得到 | 無風險 |
| 卡牌資料 | 官方公開 API 抓的 | 無風險 |
| 使用者資料 | **不存在** | 無風險 |

### 這一題最重要的一句話

**你的網站攻擊面很小，所以攻擊者不會攻擊網站，會攻擊你。**

對這個專案來說，「資安」有很大一部分其實是「**帳號安全**」：
GitHub 與 Vercel 只要有一個被登入，對方就能把任意程式碼部署到正式站，
而前面那些 CSP、fail-closed 驗證通通擋不住 —— 因為那些防的是「外人塞東西進來」，
不是「有人用你的身分改東西」。

### 待辦（只有你本人能做）

- [ ] **GitHub 開啟兩步驟驗證**，並把備用碼存在安全的地方
- [ ] **Vercel 開啟兩步驟驗證**（Vercel 用 GitHub 登入的話，就等同 GitHub 的安全性）
- [ ] 定期看一次 GitHub 的 Settings → Applications，移除不再使用的授權

---

## 二、誰能碰哪些資料？

### 使用者的資料：只在他自己的瀏覽器裡

| 資料 | 存在哪 | 誰看得到 |
|---|---|---|
| 我的收藏 | `localStorage` | 只有那台裝置的那個瀏覽器 |
| 復盤盤面 | `sessionStorage` | 同上，而且關掉分頁就沒了 |
| 牌組 | 編碼在網址裡 | **拿到網址的任何人** |

**唯一需要對使用者說清楚的是第三列**：牌組是編在網址裡分享的，
所以「分享連結」等於「公開這副牌」。這不是漏洞，是設計 ——
但使用者要知道，才不會誤以為連結是私密的。

因為沒有帳號、沒有伺服器端資料，所以**不存在越權存取（IDOR）的可能** ——
沒有「別人的資料」這種東西可以被存取。

### 程式碼與部署：誰能改上線的內容

已查證（`gh api repos/.../rules/branches/main`）：

- `main` 受 ruleset「Protect main」保護，狀態 active
- 規則：**禁止刪除**、**禁止強制推送**、**必須經由 PR**
- 必要檢查：`verify`、`audit`、`codeql`

> ⚠️ 舊的 branch-protection API 對這個儲存庫回 404「Branch not protected」，
> 那是因為改用了新的 **ruleset** 機制。查的時候別被誤導。

**但這裡有一個實際的缺口，見第四節。**

---

## 三、哪一段是我管不到的？

這一題的價值在於「知道自己在信任誰」。本站在**執行期**只信任這些：

| 對象 | 用途 | 我們做了什麼來降低依賴 |
|---|---|---|
| **Vercel** | 主機與 CDN | 沒有伺服器端邏輯，換平台的成本很低 |
| **`cmsassets.rgpub.io`** | Riot 官方卡圖 | 唯一的第三方網域；`referrerPolicy="no-referrer"`，不洩漏使用者瀏覽路徑 |
| **瀏覽器** | 執行 CSP | 不只信任 —— 三種引擎（Chromium／Firefox／WebKit）都實測 |

**執行期的第三方腳本：零。** 沒有 Google Analytics、沒有廣告、
沒有從 CDN 載入任何函式庫。已查證正式站的 CSP 是 `default-src 'none'`。

**建置期**額外信任官方內容 API 與勘誤頁面 —— 但那些資料都經過 fail-closed 驗證，
驗不過就中斷建置，不會帶著髒資料上線。

### 執行期依賴只有三個

```
next, react, react-dom
```

開發依賴 13 個（測試、型別、lint），**不會進到使用者的瀏覽器**。

依賴數量少本身就是資安措施：供應鏈攻擊的入口就是依賴，少一個就少一個入口。

---

## 四、哪些規則絕對不能被打破？

這是整份文件最重要的一節。下面每一條都是**已經用自動化擋住**或**應該擋住**的紅線。

### 已經被擋住的（有測試或 CI 把關）

| 規則 | 怎麼擋的 |
|---|---|
| 不用 `innerHTML` / `dangerouslySetInnerHTML` / `eval` / `new Function` | `npm run check:danger`，CI 必跑 |
| **不為了讓功能動而放寬 CSP** | 標頭斷言測試 + 三引擎實測零違規 |
| 外部資料驗不過就中斷建置 | `fetch-cards.mjs`、`fetch-errata.mjs` 的 fail-closed 驗證 |
| 規則相關的敘述一定附官方條號／出處 | 程式碼審查慣例 + 測試裡引用條號 |
| 不做對戰系統、不做戰績勝率統計 | Riot 開發者政策；復盤頁面有測試釘住「不是對戰系統」的定位聲明 |
| 不存個資、不用 cookie | 架構上就沒有後端 |

### ⚠️ 以為被擋住、其實沒有的：CodeQL

**這是這次盤點查出來的真缺口。**

`main` 的必要檢查是 `verify`、`audit`、`codeql`（**小寫**）。
但 GitHub 程式碼掃描產生的檢查叫 **`CodeQL`（大寫）** —— 那才是「這個 PR 有沒有帶進新的資安警告」的判定。

兩者是不同的東西：

- `codeql`（小寫）＝ **工作流程有沒有跑完**。掃描跑完就是綠的，**即使掃出高風險**。
- `CodeQL`（大寫）＝ **掃描結果**。有新的高風險警告就是紅的。

**只有小寫那個是必要檢查。** 換句話說：
**一個帶進高風險漏洞的 PR，目前在規則上是可以被合併的。**

這不是假設。本專案已經發生過兩次 `CodeQL` 紅、`codeql` 綠的情況
（PR #20 與 PR #27），兩次都是真問題不是誤報。第一次差點就這樣合併進去。

**待辦：**

- [ ] 到 GitHub → Settings → Rules → 「Protect main」，把 **`CodeQL`（大寫）** 也加進必要檢查

### ⚠️ Dependabot 只做了一半

已查證：

- `.github/dependabot.yml` **存在**，設定了每週 npm 更新、每月 Actions 更新 → 這是**版本更新**
- 但 `dependabot_security_updates` 狀態是 **`disabled`**
- `vulnerability-alerts` API 回 **404**，代表**漏洞警示未啟用**

差別在哪：

- **版本更新**（目前有）＝ 每週固定開 PR 升級套件
- **漏洞警示 + 安全性更新**（目前沒有）＝ 某個套件一爆出 CVE，**當天**就通知你並開 PR

目前的補償措施是 CI 裡的 `npm audit --audit-level=high` 與 OSV-Scanner ——
但那**只有在有人推程式碼時才會跑**。專案要是放著兩個月沒動，
期間出現的漏洞不會有人告訴你。

**待辦：**

- [ ] GitHub → Settings → Advanced Security → 開啟 **Dependabot alerts** 與 **Dependabot security updates**

### 已經做對、值得記下來的

已查證正式站回應標頭（`curl -sI`）：

```
Content-Security-Policy: default-src 'none'; script-src 'self' 'nonce-…' 'strict-dynamic'; …
Strict-Transport-Security: max-age=63072000; includeSubDomains; preload
X-Content-Type-Options: nosniff
X-Frame-Options: DENY
Referrer-Policy: strict-origin-when-cross-origin
Permissions-Policy: accelerometer=(), camera=(), geolocation=(), microphone=(), payment=(), …
Cross-Origin-Opener-Policy: same-origin
Cross-Origin-Resource-Policy: same-origin
```

秘密掃描（Secret scanning）與**推送保護**都是 `enabled` ——
推送保護特別有用：金鑰在**還沒推上去之前**就會被擋下來，而不是事後才發現。

---

## 五、外面有哪些門是開著的？

「門」＝ 任何**外部可以送資料進來**的入口。每一道門都必須有驗證。

| 門 | 誰能敲 | 驗證方式 |
|---|---|---|
| 網址查詢參數（`?d=`、`?b=`、篩選條件） | 任何人，內容任意編造 | 一律走 `decodeDeck` / `decodeBoard` / `filtersFromParams`，**逐項比對允許清單**，不認得就丟掉 |
| 貼上的牌表文字 | 使用者自己貼 | 同一套 `importDeck`，逐行比對真實卡片 |
| `localStorage` / `sessionStorage` | 使用者或同源指令碼可竄改 | **讀回來一律重新驗證**，不因為「是自己寫的」就信任 |
| 卡片圖片 | Riot CDN | `img-src` 只允許該網域；`no-referrer` |
| GitHub PR | 任何人（公開儲存庫） | 必經 PR + 必要檢查；`main` 禁止直推 |
| Vercel 預覽部署 | 有連結的人 | Vercel SSO 保護，需登入才看得到（已實測會轉址到 `vercel.com/sso-api`） |

**沒有的門**（這比有什麼門更重要）：

- 沒有 API 路由
- 沒有表單送到伺服器
- 沒有檔案上傳
- 沒有登入
- 沒有伺服器端的資料庫查詢

> 一個實際的例子：詳細頁的「回到卡牌圖鑑」連結會帶回篩選條件。
> 最直覺的寫法是把網址參數原封不動貼回去 —— 但那等於讓外人決定我們頁面上的連結。
> 實際做法是先解析成篩選狀態（只認允許清單），再重新組出網址。
> 實測 `?evil=<script>&mark=nope` 兩個都會被丟掉。

---

## 六、掃描工具現況

影片後半段講的是用工具掃描專案。本專案目前的掃描配置：

| 工具 | 抓什麼 | 何時跑 | 狀態 |
|---|---|---|---|
| `npm run check:danger` | 危險 API（`innerHTML`、`eval` 等） | 每次 CI | ✅ 必要檢查 |
| `npm audit --audit-level=high` | 依賴的已知漏洞 | 每次 CI | ✅ 必要檢查 |
| OSV-Scanner（Google） | 涵蓋比 npm audit 廣的漏洞庫 | 每次 CI | ✅ |
| CodeQL | 語意層資料流分析 | 每次 CI | ⚠️ **結果未列為必要檢查**（見第四節） |
| Secret scanning + 推送保護 | 誤commit的金鑰 | 推送時 | ✅ |
| Dependabot 版本更新 | 套件過舊 | 每週 | ✅ |
| Dependabot 漏洞警示 | 新爆出的 CVE | 即時 | ❌ **未啟用**（見第四節） |
| 單元測試（含解析器紅隊測試） | 惡意 payload 會不會被吃下去 | 每次 CI | ✅ |
| 端對端測試（三引擎） | CSP 實際攔不攔得住 | 每次 CI | ✅ |

**如果想再加一項**，我會建議 **Semgrep**（`p/javascript`、`p/react`、`p/owasp-top-ten`）——
它跟 CodeQL 抓的東西不完全重疊，而且公開儲存庫免費。

---

## 七、現在就該做的三件事

按重要性排序。**前兩件我做不到，只有你本人能操作。**

### 1. 把 `CodeQL`（大寫）加進必要檢查

GitHub → Settings → Rules → Rulesets → 「Protect main」→ Require status checks

**為什麼最優先**：這是唯一一條「你以為有、其實沒有」的防線。
其他項目是「可以更好」，這一項是「防線有洞」。

### 2. 開啟 Dependabot 漏洞警示

GitHub → Settings → Advanced Security → Dependabot alerts / security updates

**為什麼重要**：專案不可能天天有人推程式碼。沒有這個，
你只有在下次推程式碼時才會知道某個套件三週前就爆了漏洞。

### 3. 確認 GitHub 與 Vercel 都開了兩步驟驗證

**為什麼重要**：見第一節。這個網站最值錢的東西不在網站裡，在你的帳號裡。

---

## 八、什麼時候要重跑這份清單

- **每次加新功能之前** —— 特別是第五節（有沒有開新的門？）
- **每次引入新的第三方服務之前** —— 第三節（我又多信任了誰？）
- **做到帳號系統、後端 API 時** —— 整份重寫，因為前面「沒有後端所以沒有這個問題」的答案
  會全部失效。屆時請一併看 `SECURITY.md` 第六節列的原則。

---

## 誠實的但書

這份清單能排除的是**已知的常見錯誤**，那佔實務漏洞的絕大多數，但不是全部。
它真正的價值在兩件事：

1. 把「我以為有做」和「真的有做」分開 —— 這次就查出了兩個。
2. 建立回歸防線 —— 以後改壞了會被 CI 抓到，而不是上線半年後才發現。

**沒有任何檢查能保證「絕對沒問題」。**
這個專案目前最強的保障仍然是那句話：
**沒有後端、沒有使用者資料、沒有第三方腳本。**
