# 資安策略

> 這份文件跟 [`SECURITY.md`](SECURITY.md) 不一樣，兩份都要有：
>
> - **`SECURITY.md`** 是「**我們當初為什麼這樣設計**」—— 威脅模型、防護措施、踩過的取捨。
> - **這份**是「**上線前、改動前，我要問自己什麼**」—— 一套可以重複跑的思考流程，
>   加上目前查證出來的現況與缺口。

## 這份文件的框架從哪來

框架取自 Gary Chen 的〈給非技術人員的資安教學，Vibe Coding 必學的基本功〉
（<https://youtu.be/t9WA-BkLUps>）。

那支影片教的東西在業界有個名字：**威脅建模（Threat Modeling）** ——
資安專家評估軟體時最核心的思考框架。它把這套框架拆成五個白話問題，
不需要看得懂程式碼也能問。

---

## 先建立底層概念：CIA 三原則

在問五個問題之前，要先知道「資安到底在防什麼」。答案是三件事：

| 原則 | 白話 | 出事的樣子 |
|---|---|---|
| **機密性**（Confidentiality） | 資料不會被沒權限的人看到 | 使用者看到別人的私密內容 |
| **完整性**（Integrity） | 資料與規則不會被偷改 | 有人把自己的帳號改成無限點數 |
| **可用性**（Availability） | 系統隨時都能正常服務 | 伺服器被灌爆，正常使用者進不來 |

**這三個原則對本站的意義：**

- **機密性** —— 本站沒有任何私密資料。使用者的收藏與盤面只存在他自己的瀏覽器裡。
- **完整性** —— 本站沒有帳號、沒有點數、沒有可竄改的共享狀態。
  **唯一的完整性風險是「顯示錯的規則」** —— 這對一個規則查詢站來說才是真正的損害。
- **可用性** —— 靜態網站掛在 Vercel CDN 上，沒有伺服器邏輯可以打。

> 這個對照本身就是結論：**本站的資安重點跟一般 SaaS 完全不同。**
> 一般產品最怕資料外洩，本站最怕**規則講錯**。

---

## 五個問題

### 第一問：哪些東西一旦外洩或被偷，會讓我賠慘？（資產 Assets）

> 影片的比喻：你總得先知道家裡哪些東西最值錢，才知道要保護什麼。

**先講結論：這個網站本身幾乎沒有值得偷的東西。真正的要害在網站之外。**

這不是自我感覺良好，是架構決定的 —— 沒有後端、沒有資料庫、沒有帳號、
沒有 cookie、沒有伺服器端的使用者資料。攻擊者打下整個網站，
拿到最有價值的東西是**一份本來就公開的卡牌 JSON**。

所以資產盤點的重點必須**往外移**：

| 資產 | 被拿走會怎樣 | 現況 |
|---|---|---|
| **GitHub 帳號** | 可以改程式碼、改 CI、埋後門，然後自動部署上線 | ✅ 2FA（2026-09-07） |
| **Vercel 帳號** | 可以直接改部署內容、改環境變數、換網域指向 | ✅ 2FA（2026-09-07） |
| **網域控制權**（`ashvigil.com`，Porkbun） | 可以把整個網站導到別的地方、收走網域 | ✅ 見下方「網域的保護措施」 |
| **本站的規則正確性** | 使用者照錯的規則打牌、帶違規牌組去比賽 | 見第四問的安全憲法 |
| 原始碼 | 公開儲存庫，本來就看得到 | 無風險 |
| 卡牌資料 | 官方公開 API 抓的 | 無風險 |
| 使用者個資 | **不存在** | 無風險 |

影片提到 Vibe Coding 最典型的資產是「**串接 AI 服務的 API 金鑰**」——
綁著信用卡，外洩就是幾十萬的帳單。**本站沒有任何 API 金鑰、沒有任何付費服務**，
這一類風險完全不存在。

#### 帳號的保護措施（2026-09-07 完成）

| 項目 | 狀態 |
|---|---|
| GitHub 兩步驟驗證 | ✅ 已啟用 |
| Vercel 兩步驟驗證 | ✅ 已啟用 |
| 救援碼存放 | **紙本（實體保管）＋ Bitwarden 安全筆記** |

> **本專案不存放任何救援碼、密碼或金鑰。** 這裡只記錄「東西放在哪」，不記錄內容 ——
> 救援碼的作用是「不需要 2FA 也能登入」，把它放進公開儲存庫等於直接公開帳號。
>
> 救援碼刻意存兩份、而且是**不同介質**：紙本是最後防線（換手機、斷網、電腦壞掉都拿得到），
> Bitwarden 是日常查閱用。只放密碼管理器會有死結 ——
> 手機掉了要用救援碼登入 GitHub，但救援碼鎖在同一支手機的 App 裡。
>
> 同理，**Bitwarden 的主密碼只存在紙本那一份**，不能鎖在它自己裡面。

#### 網域的保護措施（2026-09-15 完成）

網域 `ashvigil.com` 在 Porkbun 註冊，DNS 也由 Porkbun 代管，指向 Vercel。

| 項目 | 狀態 |
|---|---|
| Porkbun 登入 | 安全金鑰（電腦 Windows Hello ＋ 手機備援），備用碼：紙本 ＋ Bitwarden 安全筆記 |
| Bitwarden 本身 | 兩步驟驗證（FIDO2 安全金鑰）；它的救援碼**只放紙本** |
| 網域鎖（Domain Lock） | ✅ 開啟 —— 沒解鎖就不能被轉到別家註冊商 |
| 聯絡資料隱私 | ✅ 開啟 —— 公開查詢看不到姓名、住址、電話 |
| DNSSEC | ✅ 開啟 —— 別人無法偽造本站的 DNS 回應 |
| 自動續約 | ✅ 開啟（到期日 2027-09-15）；另設行事曆提醒，防信用卡過期扣款失敗 |
| CAA 紀錄 | 只允許 Let's Encrypt 簽發憑證 |
| 寄信防偽 | 本網域不寄信：null MX ＋ SPF `-all` ＋ DMARC `p=reject`，別人冒用網域寄的信會被擋 |
| ICANN 註冊人驗證 | ✅ 2026-09-17 Porkbun 客服確認無待驗證事項（工單 #32198561） |

> **換註冊商或換 DNS 代管之前，要先關掉 DNSSEC。**
> 否則新的 DNS 伺服器沒有對應的簽章，驗證失敗的瀏覽器會整站打不開。
>
> 網域被搶走是本站最難救回的事故：程式碼在 GitHub 有備份、部署可以重來，
> 但網域一旦被轉走，就得跟註冊商走申訴流程，期間所有連結都指向別人。
> 所以 Porkbun 的保護等級比照 GitHub 與 Vercel。

#### 這一問對本站最重要的一句話

**你的網站攻擊面很小，所以攻擊者不會攻擊網站，會攻擊你。**

GitHub 與 Vercel 只要有一個被登入，對方就能把任意程式碼部署到正式站，
而 CSP、fail-closed 驗證通通擋不住 —— 那些防的是「外人塞東西進來」，
不是「有人用你的身分改東西」。

---

### 第二問：會來哪些人？每個人能碰哪些資產？（身分驗證與授權）

> 影片的重點：**身分驗證（Authentication）跟授權（Authorization）是兩道防線，缺一不可。**
> 前者管「你能不能進門」，後者管「你進門之後有沒有資格碰眼前的東西」。
>
> 最常見的盲點：串好 Google 登入、沒登入的人進不來，就以為安全了 ——
> **那只做到了第一步。**

**本站的答案很短：只有一種角色，就是「訪客」。**

沒有註冊、沒有登入、沒有付費層級、沒有管理後台。所以：

- **沒有身分驗證** —— 因為不需要辨識任何人
- **沒有授權** —— 因為沒有「別人的資料」這種東西可以被越權存取

也就是說，**IDOR（越權存取）這個最常見、最容易被漏掉的漏洞，在本站架構上不可能發生**。

使用者的資料分佈：

| 資料 | 存在哪 | 誰看得到 |
|---|---|---|
| 我的收藏 | `localStorage` | 只有那台裝置的那個瀏覽器 |
| 復盤盤面 | `sessionStorage` | 同上，關掉分頁就沒了 |
| 牌組 | 編碼在網址裡 | **拿到網址的任何人** |

**唯一要對使用者說清楚的是第三列**：牌組是編在網址裡分享的，
所以「分享連結」等於「公開這副牌」。這不是漏洞，是設計 —— 但使用者要知道。

#### 什麼時候這一問會變成真正的難題

**做到帳號系統（路線圖第 8 階段）的那一天。** 屆時必須同時做對兩件事：

1. 身分驗證委外給 OAuth，絕不自己存密碼
2. **每一次撈資料都要檢查「這筆資料屬不屬於這個人」** ——
   不是只檢查「這個人有沒有登入」

第 2 點就是影片講的那個致命盲點。`SECURITY.md` 第六節已經寫進這條原則。

---

### 第三問：資料一路會經過哪些地方？哪裡我掌控不了？（信任邊界 Trust Boundary）

> 影片的定義：當資料從**不可控的外部**跨進**你能控制的後端**，
> 那道分界線就叫信任邊界。
>
> 核心鐵律：**所有跟錢、點數、權限有關的計算，一律要在後端親自重算，
> 絕不能盲目相信前端送來的任何數字。**

**本站的信任邊界在哪裡？** 這是本站最特別的地方 ——

```
建置時（我的電腦 / GitHub Actions）        執行時（使用者的瀏覽器）
┌────────────────────────────┐        ┌──────────────────────────┐
│ 官方 API → 嚴格驗證 → 靜態 JSON │  ───►  │ 全部運算都在這裡         │
│ 驗不過就中斷建置              │        │ 沒有任何資料回傳給我們   │
└────────────────────────────┘        └──────────────────────────┘
         ▲ 真正的信任邊界在這裡                    ▲ 沒有伺服器可以被騙
```

**本站沒有執行期的信任邊界，因為沒有後端。** 使用者在瀏覽器裡做的任何事
（改網址、改 localStorage、開 DevTools 改數字）**只會影響他自己**，
不會影響任何其他人，也不會讓我們損失任何東西。

所以影片那條「跟錢有關的計算要在後端重算」的鐵律，本站**沒有東西適用** ——
因為沒有錢、沒有點數、沒有共享狀態。

**但信任邊界另一邊真實存在：建置時的外部資料。**

| 進來的東西 | 從哪來 | 邊界上做了什麼檢查 |
|---|---|---|
| 卡牌資料 | Riot 官方 API | Zod 驗證 + HTML 解析成結構化 token；驗不過**中斷建置** |
| 勘誤表 | 官方 Rules Hub 網頁 | 嚴格解析 + 純文字斷言；對不到卡片**中斷建置** |
| 卡圖 | 官方 CDN（建置時下載，本站代管） | 下載失敗就中斷建置；執行期 CSP 的 `img-src` 只有本站 |
| npm 套件 | npm registry | 執行期只有 3 個（next / react / react-dom） |

#### 我們管不到的部分（誠實列出）

- **Vercel** —— 主機與 CDN。沒有伺服器端邏輯，換平台成本很低
- **GitHub** —— 原始碼與 CI
- **Riot CDN** —— 卡圖。若它被入侵並送出惡意圖片，我們擋不住（但 CSP 限制它只能是圖片）
- **瀏覽器** —— 我們的 CSP 保證建立在「瀏覽器正確實作規格」之上。
  所以不只信任 —— 三種引擎（Chromium／Firefox／WebKit）都實測

---

### 第四問：有哪些底線是「絕對不能被打破的鐵律」？（安全不變條件 Security Invariants）

> 影片講得最好的一段：**這是你產品的最高安全憲法。**
>
> 而且對用 AI 寫程式的人特別重要 —— 你可能看不懂底層程式碼，
> **但講得出這幾條憲法並不困難**。講得出來，就能請 AI 照著它審查全部程式碼。

以下是**本站的安全憲法**。每一條都附上目前是靠什麼在守。

#### 第一條：使用者瀏覽器裡的任何東西，永遠不可信

網址參數、`localStorage`、`sessionStorage`、貼上的牌表文字 ——
**一律重新驗證，逐項比對允許清單，不認得就丟掉。**

不因為「這是我們自己寫進去的」就信任讀回來的值。

> 守門的：`decodeDeck` / `decodeBoard` / `filtersFromParams` / `importDeck`，
> 全部走 fail-closed。實測 `?evil=<script>&mark=nope` 兩個都會被丟掉。

#### 第二條：外部資料驗不過，就中斷建置 —— 絕不「盡量解析」

寧可整個建置失敗，也不要讓一筆猜出來的資料上線。

> 守門的：`fetch-cards.mjs` 的 Zod 驗證與 HTML token 解析、
> `fetch-errata.mjs` 的純文字斷言與卡片對應檢查。
> **這條實際救過一次**：勘誤解析寫錯時，「新舊文字一樣就中止」的斷言
> 擋下了 31 筆全錯的資料。

#### 第三條：絕不為了讓功能動而放寬 CSP

功能跟 CSP 衝突時，**改的是程式碼，不是 CSP**。

> 守門的：標頭斷言測試（斷言不含 `unsafe-inline` / `unsafe-eval`）
> ＋ 三引擎實測零違規。
> **這條也實際擋過**：問號說明面板原本被捲動容器裁掉，
> 解法是改用 `position: fixed`，而不是放寬 `style-src`。

#### 第四條：任何規則敘述都必須附官方出處

規則條號、禁卡表版本、勘誤日期 —— 使用者要能自己查證。

**這是本站最特殊的一條憲法**，因為本站的核心資產就是「規則正確性」。
一般網站怕資料外洩，本站怕**講錯規則**。

> 守門的：`checkLegality` 每條訊息都帶 `rule` 欄位；
> 禁卡與勘誤都標版本日期與官方連結；測試會斷言這些欄位存在。

#### 第五條：不蒐集個資、不用 cookie、不放第三方執行期腳本

> 守門的：架構上就沒有後端；CSP 是 `default-src 'none'`；
> 端對端測試斷言收藏功能不發出任何網路請求。

#### 第六條：不做對戰系統、不做對局戰績與勝率統計

這條不是技術限制，是 **Riot 開發者政策**。

> 守門的：復盤頁面有測試釘住「這是研究工具，不是對戰系統」的定位聲明，
> 擋住未來有人「為了省空間」把它藏起來。

---

#### 為什麼要把憲法寫出來：給 AI 當檢查依據

影片提到一個很實用的做法：**把這幾條憲法寫進專案裡的 `SECURITY.md`，
AI 資安掃描工具每次掃描時會自動讀取它**，就能照著你的規則去檢查程式碼，
而不是只找通用的漏洞樣式。

你也可以在請 AI 檢查時，直接把上面六條貼進對話框，說：

> 這是我的最高安全憲法，請幫我審查所有程式碼，找出任何會打破這些規則的漏洞。

**這對本站特別有效**，因為第四條（規則必須附出處）是通用掃描器
**永遠不會知道**的規則 —— 那是我們這個產品獨有的。

---

### 第五問：如果有人想搞垮我，最可能從哪裡下手？（攻擊面 Attack Surface）

> 影片的比喻：守一座城堡，你總得先知道對外開了哪些大門、側門、送貨口。
>
> 定義：所有使用者可以輸入文字、上傳檔案、觸發運算的地方，都叫攻擊面。

| 門 | 誰能敲 | 裝了什麼防禦 |
|---|---|---|
| 網址查詢參數（`?d=`、`?b=`、篩選條件） | 任何人，內容任意編造 | 逐項比對允許清單，不認得就丟掉 |
| 貼上的牌表文字 | 使用者自己 | 逐行比對真實卡片，認不出來會列出而不是靜靜吞掉 |
| `localStorage` / `sessionStorage` | 使用者或同源指令碼 | 讀回來一律重新驗證 |
| 卡片圖片 | 本站（2026-09-12 起代管） | `img-src` 只允許本站，瀏覽器不連任何外部伺服器 |
| GitHub PR | 任何人（公開儲存庫） | 必經 PR + 必要檢查；`main` 禁止直推 |
| Vercel 預覽部署 | 有連結的人 | Vercel SSO 保護，需登入（已實測會轉址到 `sso-api`） |

**沒有的門**（這比有什麼門更重要）：

- 沒有 API 路由
- 沒有表單送到伺服器
- **沒有檔案上傳** —— 影片提到的「有人丟幾十 GB 檔案塞爆伺服器」不適用
- 沒有登入 —— 影片提到的「腳本狂洗 1000 個免洗帳號」不適用
- **沒有會花錢的運算** —— 影片提到的「API 帳單半小時被吸乾幾千美金」不適用
- 沒有伺服器端資料庫查詢

> 影片提到的三種典型防禦（真人驗證、Rate Limit、檔案大小限制）
> **本站一個都不需要**，因為對應的門根本沒開。這是架構帶來的紅利。

---

## 貫穿全部的核心思維：假設外圍已經被突破

> 影片講的：**永遠要假設外圍防護都已經被突破了，
> 你最核心的資產必須具備「自我防衛」的能力。**
>
> 影片舉的例子是資料庫的 RLS（Row Level Security）——
> 不管請求從哪裡來，資料庫最底層只准交出屬於當前使用者的資料。

**本站已經有一層這樣的設計，值得指名：**

CSP 是外圍防護。但就算 CSP 完全失效，**本站也沒有 HTML 可以被注入** ——
因為卡牌能力文字在**建置時**就被解析成結構化 token，
專案裡從頭到尾沒有一個 `dangerouslySetInnerHTML`。

也就是說：**危險的東西根本沒有進到專案裡**，而不是「進來了但被擋住」。
這就是核心資產的自我防衛能力。

**未來加後端時要記得這個思維**：不要只靠應用層的權限檢查，
資料庫本身也要設 RLS。應用層寫錯了，資料庫還會擋一次。

---

## 現況查證

以下每一項都是實際查過的，不是照文件抄的。

### ✅ 已經做對的

| 項目 | 查證方式 | 結果 |
|---|---|---|
| 八個安全標頭 | `curl -sI` 正式站 | 全部存在，CSP 是 `default-src 'none'` |
| `main` 分支保護 | `gh api .../rules/branches/main` | ruleset「Protect main」active：禁刪除、禁強推、必經 PR |
| 必要檢查 | 同上 | `verify`、`audit`、`codeql` |
| 秘密掃描 | `gh api repos/...` | `enabled`，**推送保護也開著**（金鑰推上去之前就擋） |
| 執行期依賴 | `package.json` | 只有 3 個：next / react / react-dom |
| 第三方執行期腳本 | CSP + DevTools | 零 |

### ⚠️ 缺口一：CodeQL 的必要檢查掛錯了

`main` 的必要檢查是 `verify`、`audit`、`codeql`（**小寫**）。

但兩者是不同的東西：

- **`codeql`（小寫）** ＝ 工作流程有沒有跑完。**掃出高風險它照樣是綠的。**
- **`CodeQL`（大寫）** ＝ GitHub 程式碼掃描的**結果**。有新的高風險警告才會紅。

**只有小寫那個是必要檢查。** 換句話說：
**一個帶進高風險漏洞的 PR，目前在規則上是可以被合併的。**

這不是假設。本專案已經發生過兩次 `CodeQL` 紅、`codeql` 綠
（PR #20 與 PR #27），兩次都是真問題不是誤報，第一次差點就這樣合併進去。

用第四問的話講：**「紅燈不合併」這條你以為有的憲法，其實沒有被守住。**

### ⚠️ 缺口二：Dependabot 只做了一半

- `.github/dependabot.yml` **存在** → 這是**版本更新**（每週固定開 PR）
- `dependabot_security_updates` → **`disabled`**
- `vulnerability-alerts` API → **404（未啟用）**

差別：版本更新是「每週例行升級」；漏洞警示是「**某個套件今天爆出 CVE，
當天就通知你**」。目前只靠 CI 的 `npm audit` 與 OSV-Scanner 頂著，
但那**只有推程式碼時才會跑**。專案放兩個月沒動，期間的漏洞不會有人告訴你。

### ⚠️ 缺口三：沒有把安全憲法交給 AI 掃描

現有的 `SECURITY.md` 是**設計理由文件**，寫給人看的。
上面第四問那六條憲法是這次才整理出來的 ——
在此之前，AI 或掃描工具**無從得知本站獨有的規則**
（尤其是「規則敘述必須附官方出處」這條，通用掃描器永遠不會知道）。

---

## 掃描工具現況

| 工具 | 抓什麼 | 何時跑 | 狀態 |
|---|---|---|---|
| `npm run check:danger` | 危險 API（`innerHTML`、`eval` 等） | 每次 CI | ✅ 必要檢查 |
| `npm audit --audit-level=high` | 依賴的已知漏洞 | 每次 CI | ✅ 必要檢查 |
| OSV-Scanner（Google） | 涵蓋比 npm audit 廣的漏洞庫 | 每次 CI | ✅ |
| CodeQL | 語意層資料流分析 | 每次 CI | ⚠️ **結果未列為必要檢查** |
| Secret scanning + 推送保護 | 誤 commit 的金鑰 | 推送時 | ✅ |
| Dependabot 版本更新 | 套件過舊 | 每週 | ✅ |
| Dependabot 漏洞警示 | 新爆出的 CVE | 即時 | ❌ **未啟用** |
| 單元測試（含解析器紅隊測試） | 惡意 payload 會不會被吃下去 | 每次 CI | ✅ |
| 端對端測試（三引擎） | CSP 實際攔不攔得住 | 每次 CI | ✅ |
| **AI 資安審查** | 針對本站憲法的邏輯漏洞 | — | ❌ **尚未納入流程** |

---

## 建議的四步驟流程

影片示範的操作流程，換成本專案的做法：

| 步驟 | 什麼時候 | 做什麼 |
|---|---|---|
| **1. 全盤掃描** | 專案階段性完成時 | 請 AI 拿著上面六條憲法審查整個專案 |
| **2. 修補** | 報告列出漏洞後 | 逐項修，每項都要能講出「為什麼這樣修」 |
| **3. 驗證修補** | 修完之後 | 沿著同一條攻擊路徑再確認一次，**並補一條測試把它釘住** |
| **4. 改版檢查** | 每次加功能／改程式碼 | 只檢查這次新寫的部分，確保沒帶進新漏洞 |

**第 3 步的「補一條測試」是本專案自己加的。** 影片講的是重新驗證，
但只驗證一次的話，下次改壞了不會有人知道。本專案的慣例是
**每個修好的問題都留一條測試當回歸防線** ——
`html-text.test.ts` 那 10 條紅隊測試就是這樣來的。

---

## 什麼時候要重跑這五個問題

- **每次加新功能之前** —— 特別是第五問（有沒有開新的門？）
- **每次引入新的第三方服務之前** —— 第三問（信任邊界移動了嗎？）
- **做到帳號系統、後端 API 時** —— **整份重寫**。
  前面「沒有後端所以沒有這個問題」的答案會全部失效，
  第二問（身分驗證 vs 授權）與第三問（信任邊界）會從「不適用」變成最難的部分。

---

## 誠實的但書

這套流程能排除的是**已知的常見錯誤**，那佔實務漏洞的絕大多數，但不是全部。
它真正的價值在兩件事：

1. **把「我以為有做」和「真的有做」分開** —— 這次就查出了三個缺口。
2. **建立回歸防線** —— 以後改壞了會被 CI 抓到，而不是上線半年後才發現。

**沒有任何檢查能保證「絕對沒問題」。**
這個專案目前最強的保障仍然是那句話：
**沒有後端、沒有使用者資料、沒有第三方腳本。**
